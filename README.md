# Offline-Voice-Assistant-with-Machine-Learning

Required:

python => 3.8

Unpack a language model vosk into a project

pip install vosk

pip install sounddevice

pip install scikit-learn

pip install pyttsx3

pip install requests
